#!/usr/bin/env bash
set -euo pipefail

case "${CLIPBOARD_STATE:-data}" in
  data)
    rm -f "${XDG_CACHE_HOME:-$HOME/.cache}/cliphist/cleared-by-waybar"
    exec cliphist store
    ;;
  *)
    exit 0
    ;;
esac
